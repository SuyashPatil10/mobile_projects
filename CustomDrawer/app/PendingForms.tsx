import { Text, View } from "react-native";

function PendingForms() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Pending Forms Screen</Text>
        </View>
    );
}

export default PendingForms;